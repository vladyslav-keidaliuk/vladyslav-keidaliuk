{
  "scripts": {
    "dev": "npm run dev --prefix remix-app",
    "build": "npm run build --prefix remix-app",
    "start": "npm run start --prefix remix-app",
    "typecheck": "npm run typecheck --prefix remix-app"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.16"
  }
}
